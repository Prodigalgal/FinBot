# One proxy per line. Supported schemes depend on httpx and your environment.
# Examples:
# socks5://user:password@127.0.0.1:1080
# http://user:password@127.0.0.1:8080
#
# Note: use socks5h:// only when the target service has proxy-side DNS/IPv6
# reachability. Some IPv6 proxy pools fail on IPv4-only domains when remote DNS
# is forced.
#
# Set FIRECRAWL_PROXY_FILE=config/firecrawl_proxies.txt to use a real file.
# Keep the real file out of version control.
# Firecrawl requests must use this proxy pool; direct fallback is disabled.
